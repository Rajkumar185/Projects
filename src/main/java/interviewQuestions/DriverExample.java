package interviewQuestions;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class DriverExample {
	
	//WebDriver driver =new WebDriver();// Not Possible we cannot able to create an object for the interface

	ChromeDriver driver=new ChromeDriver();//--> Possible as Chromedriver is a class
}
